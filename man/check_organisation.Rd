% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_organisation.R
\name{check_organisation}
\alias{check_organisation}
\title{Prüft Organisations-Variable auf definierte Qualitätsregeln}
\usage{
check_organisation(
  data,
  organisation_col = "organisation",
  semester_col = "semester",
  stop_at = 1,
  show_report = TRUE
)
}
\arguments{
\item{data}{Ein \code{data.frame}, das die zu prüfende Organisations-Variable enthält.}

\item{organisation_col}{Name der Organisations-Variable (Standard: \code{organisation}).}

\item{semester_col}{Name der Semester-Spalte (Standard: \code{semester}).
Wird für den Check auf Schwankungen zwischen Semestern benötigt.}

\item{stop_at}{Schwellenwert für Fehler-Toleranz. Kann relativ (Anteil 0 bis 1)
oder absolut (z. B. 1 für "Null Toleranz") angegeben werden.
Standard: 1 (= Null Toleranz).}

\item{show_report}{Logisch; wenn \code{TRUE}, wird ein pointblank-Report
mit Badges im Viewer ausgegeben. Standard: \code{TRUE}.}
}
\value{
Ein \code{pointblank}-Agent-Objekt (invisible). Über
\code{pointblank::get_agent_report()} kann der Report auch separat erzeugt werden.
}
\description{
Diese Funktion verwendet das Paket \code{pointblank}, um die Werte in der
Organisations-Variable systematisch zu validieren. Optional wird ein HTML-Report mit Badges ausgegeben.
}
\details{
Die folgenden Prüfungen werden durchgeführt:
\enumerate{
\item Nur korrektes Semikolon " ; " oder kein Semikolon.
\item Kein "|" enthalten.
\item Kein ">" enthalten.
\item Länge der Zeichenkette ist kleiner oder gleich 1000.
\item Keine überflüssigen Leerzeichen (entspricht \code{stringr::str_squish()}).
\item Warnung bei Abschluss-Begriffen (z. B. Bachelor, Master, Diplom).
}
}
\examples{
\dontrun{
  agent <- check_organisation(test_data, organisation_col = "organisation")
  pointblank::get_agent_report(agent)
}

}
