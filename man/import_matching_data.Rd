% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_matching_data.R
\name{import_matching_data}
\alias{import_matching_data}
\title{Lädt das aktuellste Matching-\code{.rds}-File aus dem \code{Matching}-Ordner der
angegebenen Universität (lokal in OneDrive) und merged die relevanten Matching-Felder mit \code{scraped_daten}.
und merged die relevanten Matching-Felder mit \code{scraped_daten}. Wenn kein
passendes Matching vorhanden ist oder die Matching-Daten nicht LUF-
optimiert sind, wird \code{scraped_daten} unverändert zurückgegeben.}
\usage{
import_matching_data(scraped_daten, uni_ordner_name)
}
\arguments{
\item{scraped_daten}{Data frame mit gescrapten Daten. Erwartet mindestens
die Spalten \code{jahr}, \code{semester} und \code{organisation}.}

\item{uni_ordner_name}{Character. Name des Universitätsordners innerhalb der
\code{single_universities}-Struktur.
Hinweis: Die Funktion lädt Matching-Dateien aus dem lokalen OneDrive.}
}
\value{
Data frame. \code{scraped_daten} mit angehängten Matching-Feldern
(\code{matchingart}, \code{lehr_und_forschungsbereich}, \code{studienbereich},
\code{faechergruppe}, \code{luf_code}, \code{stub_code}, \code{fg_code}). Bei fehlendem oder
ungeeignetem Matching werden die unveränderten \code{scraped_daten}
zurückgegeben.
}
\description{
Lädt das aktuellste Matching-\code{.rds}-File aus dem \code{Matching}-Ordner der
angegebenen Universität (lokal in OneDrive) und merged die relevanten Matching-Felder mit \code{scraped_daten}.
und merged die relevanten Matching-Felder mit \code{scraped_daten}. Wenn kein
passendes Matching vorhanden ist oder die Matching-Daten nicht LUF-
optimiert sind, wird \code{scraped_daten} unverändert zurückgegeben.
}
\details{
\itemize{
\item Es wird das jüngste \code{.rds}-File im \code{Matching}-Ordner verwendet.
\item Die Matching-Daten müssen die Spalte \code{gerit_luf} enthalten.
\item Organisationseinträge mit mehreren Namen werden anhand von " ; "
getrennt und entsprechend behandelt.
}
}
