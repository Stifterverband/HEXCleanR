% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_missing_languages.R
\name{detect_missing_languages}
\alias{detect_missing_languages}
\title{Ergänzt fehlende Sprachinformationen in Kursdaten}
\usage{
detect_missing_languages(
  raw_data,
  db_data_path,
  export_path = "db_safety_export.rds",
  batch_size = 100,
  titel_col = "titel",
  kursbeschreibung_col = "kursbeschreibung",
  sprache_col = "sprache_recoded",
  kursbeschreibung_sprach_col = "kursbeschreibung_sprach"
)
}
\arguments{
\item{raw_data}{Ein data.frame oder tibble mit den Kursdaten.}

\item{db_data_path}{Pfad zur RDS-Datei mit bestehenden Sprachklassifikationen
für \code{detect_lang_with_openai()}.}

\item{export_path}{Pfad zum Sicherheits-Export für
\code{detect_lang_with_openai()}. Standard ist \code{"db_safety_export.rds"}.}

\item{batch_size}{Batch-Größe für \code{detect_lang_with_openai()}. Standard ist
\code{100}.}

\item{titel_col}{Name der Titelspalte. Standard ist \code{"titel"}.}

\item{kursbeschreibung_col}{Name der Spalte mit Kursbeschreibungen.
Standard ist \code{"kursbeschreibung"}.}

\item{sprache_col}{Name der Zielspalte für die recodierte Sprache. Standard
ist \code{"sprache_recoded"}.}

\item{kursbeschreibung_sprach_col}{Name der Spalte für die per \code{cld3}
erkannte Sprache der Kursbeschreibung. Standard ist
\code{"kursbeschreibung_sprach"}.}
}
\value{
\code{raw_data} mit ergänzten Spalten \code{sprache_recoded} und
\code{kursbeschreibung_sprach}.
}
\description{
Die Funktion behandelt zwei Fälle für Zeilen mit fehlendem
\code{sprache_recoded}:
0. Falls \code{db_data_path} existiert, werden vorhandene gültige Werte in
\code{sprache_recoded} zunächst anhand von \code{titel} aus der DB übernommen.
Bereits vorhandene Werte in \code{raw_data} werden dabei nicht überschrieben.
\enumerate{
\item Wenn nur ein \code{titel} vorliegt und \code{kursbeschreibung} fehlt, wird
\code{detect_lang_with_openai()} auf diese Zeilen angewendet.
\item Wenn \code{kursbeschreibung} vorhanden ist, wird die Sprache der
Kursbeschreibung mit \code{cld3::detect_language()} bestimmt und in
\code{kursbeschreibung_sprach} geschrieben.
}
}
\details{
Bestehende Werte in \code{sprache_recoded} und \code{kursbeschreibung_sprach} werden
nicht überschrieben.
}
