% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_baby_dbs.R
\name{create_baby_dbs}
\alias{create_baby_dbs}
\title{Speichert semesterweise Teilmengen von \code{db_data} als RDS-Dateien}
\usage{
create_baby_dbs(db_data, path)
}
\arguments{
\item{db_data}{Data Frame mit mindestens einer Spalte \code{semester}.}

\item{path}{Basisverzeichnis, unter dem pro Semester ein Unterordner erwartet
wird.}
}
\value{
Unsichtbar eine Liste mit zwei Zeichenvektoren:
\describe{
\item{saved}{Dateipfade erfolgreich gespeicherter RDS-Dateien.}
\item{missing_dirs}{Ordnerpfade, die nicht existierten.}
}
}
\description{
Diese Funktion iteriert ueber alle eindeutigen Werte in \code{db_data$semester}
und speichert pro Semester eine Teilmenge als \verb{db_data_<semester>.rds} in den
passenden Unterordner unterhalb von \code{path}. Gespeichert wird nur, wenn der
jeweilige Ordner bereits existiert.
}
\examples{
db_data <- tibble::tibble(
  semester = c("2025w", "2025w", "2026s"),
  titel = c("Kurs A", "Kurs B", "Kurs C")
)

base_path <- tempdir()
dir.create(file.path(base_path, "2025w"))
dir.create(file.path(base_path, "2026s"))

create_baby_dbs(db_data, base_path)

}
